using UnityEngine;
using System.Collections;
using UnityEngine.Networking;

public class ShootingCheck : NetworkBehaviour {

    /*[SyncVar]
    public bool PlayerPressed;

	// Use this for initialization
	void Start () {
        PlayerPressed = false;
	}

    // Update is called once per frame
    void Update()
    {
        if (PlayerPressed == true)
        {
            PlayerPressed = false;
        }
    */ 
}
